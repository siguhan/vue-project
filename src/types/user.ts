export interface LoginDto {
    id: string
    username: string
    email: string
    token: string
    isLogin: boolean
}

export interface UserInfo {
    id: string
    username: string
    email: string
    isLogin: boolean
}
