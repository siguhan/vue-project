组件里用 useRouter()，组件外用直接导入 router 实例

Volar 是一个 Vue 3 官方推荐的语言服务工具（Language Server），主要用于 编辑器里的代码提示、类型检查和错误提示，尤其是 TypeScript + Vue 3 场景。

height: 100% 只能基于父元素的 height 计算，不能基于 min-height，所以父元素只有 min-height: 100vh 时，子元素的 height: 100% 不会生效。

HTTP 状态码：告诉“请求有没有成功到达服务器”
业务码（Business Code）：告诉“业务做得对不对”

Element Plus 的 validate 一旦使用回调模式，就不会通过 Promise 抛错，await 失效；只有使用不带回调的 Promise 模式，校验失败才会 reject，流程才能被 await + try/catch 正确中断。

Java 的 Long 在前端不能直接用 number 表示，因为 JS 的 number 有 2^53 的安全整数限制，超过会丢失精度。
实际项目中一般由后端把 Long 转成字符串返回，前端用 string 接收，只作为标识使用，这是最安全、最常见的方案。

登录场景不用防抖，用「loading 锁 + 禁用按钮」，保证请求完成前只能提交一次；必要时在 axios 拦截器层做全局重复请求控制。

Grid 的直接子元素都会成为 grid item
overlay 也是 .container 的 直接子元素
👉 它被当成 一个 grid item
👉 即使 v-if 为 false，grid 轨道计算已经被你破坏过
你让一个“不该参与布局的元素”成为了 grid item，从而触发了 CSS Grid 的自动放置 / 轨道分配规则，导致整体布局结果发生变化。

先放置有明确 grid-area / grid-row / grid-column 的元素
再对「没有指定位置的元素」执行 自动放置（auto-placement）

overlay 的特点：
是 .container 的直接子元素 → grid item
没有 grid-area

1️⃣ Grid 先放置：
sidebar → grid-area: sidebar
header → grid-area: header
main → grid-area: main
2️⃣ 发现还有一个 未定位的 grid item（overlay）
3️⃣ 启动 自动放置算法

roles 👉 用于 路由级
permissions 👉 用于 按钮级

router.beforeEach 在“每一次导航开始之前”执行，
并且发生在组件创建、setup、render 之前

SVG 默认尺寸单位是 em

当你点击一个 非 input 元素（这里是 span）时，浏览器会先 更新焦点：
input 失焦（blur）
这个是 同步触发的焦点事件，在浏览器处理点击前触发。
因此 blur 会在点击事件之前触发。
事件触发顺序（事件冒泡）
点击的元素触发 click 事件（span click）
然后事件冒泡到父元素（div click）

当你在一个已获得焦点的元素（如输入框）上按下鼠标，然后释放到其他位置时：
在原元素上触发 mousedown
如果鼠标释放时在另一个可获得焦点的元素上：
原元素触发 blur
新元素触发 focus
最后触发 mouseup 和 click

在 mousedown 中阻止默认行为
el.addEventListener('mousedown', e => {
e.preventDefault()
})
结果：
mousedown
mouseup
click
👉 不会触发 blur / focus

多次点击 同一个 input
✅ 触发的事件顺序
mousedown
mouseup
click
👉 不会触发：
❌ blur
❌ focus
原因
焦点没有发生变化
