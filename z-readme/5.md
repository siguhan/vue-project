```js
error
;[
    {
        message: '用户名不能为空',
        field: 'username',
    },
]

fields
{
    username: [
        {
            message: '用户名不能为空',
            field: 'username',
        },
    ]
}
```

| 名称         | 作用                  |
| ------------ | --------------------- |
| `descriptor` | 校验规则（rule 定义） |
| `model`      | 要校验的数据对象      |
| `Schema`     | 校验器实例            |

required, type, min / max, pattern(正则), enum
{ type: 'string' }
{ type: 'number' }
{ type: 'boolean' }
{ type: 'array' }
{ type: 'email' }
{ type: 'url' }

先 change 回调 -> 后 blur 回调

调用 validate()
立即返回一个 Promise（pending）
JS 引擎在当前调用栈结束后
把 resolve(true) 放进微任务队列
微任务执行 → Promise 才变成 fulfilled
